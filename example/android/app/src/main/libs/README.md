This folder is populated automatically by the library postinstall step.

Bundled Samsung S Pen Remote SDK files:

- `spenremote-v1.0.1.jar`
- `sdk-v1.0.0.jar`

The app Gradle file already includes `fileTree(dir: "libs", include: ["*.jar", "*.aar"])`.
